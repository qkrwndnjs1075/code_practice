/*#include <stdio.h>

int main() {
    char *str = "He Is My Best Friend!";
    int big = 0;

    while (*str != '\0') {
        if (*str >= 'A' && *str <= 'Z') {
            big++;
        }
        str++;
    }

    printf("대문자 개수: %d\n", big);

    return 0;
}
*/